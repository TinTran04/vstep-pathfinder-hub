services:
  vai.api:
    build:
      context: .
      dockerfile: VAIApplication/Dockerfile
    container_name: vai.api
    env_file:
      - .env
    environment:
      ASPNETCORE_URLS: http://+:8080
    ports:
      - "8080:8080"
    restart: unless-stopped
